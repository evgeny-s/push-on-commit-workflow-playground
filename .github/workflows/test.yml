name: Test push/PR

on:
  push:
    branches:
       - main
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello world
        run: echo "Hello world"